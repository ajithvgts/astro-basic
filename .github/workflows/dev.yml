name: Dev

on:
  push:
    branches: main

jobs:
  build:
    runs-on: self-hosted

    steps:
      - name: Clone Repo
        uses: actions/checkout@v4
        with:
          path: "dev"

      - name: Install dependencies
        run: pnpm install
      - name: Build
        run: pnpm run build
